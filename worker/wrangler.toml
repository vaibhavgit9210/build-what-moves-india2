name = "sahayata-help"
main = "worker.js"
compatibility_date = "2026-08-01"

# Secrets (set with `wrangler secret put`):
#   ANTHROPIC_API_KEY - required; without it the worker answers 503 and the
#                       frontend falls back to its clearly-labeled demo answers.
